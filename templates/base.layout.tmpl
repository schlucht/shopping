{{define "base"}}
<!doctype html>
<html lang="en">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>Shopping Seite</title>

    <link rel="stylesheet" type="text/css" href="/static/css/style.min.css">
</head>

<body>
    {{ block "header" .}}{{ end }}
    <main>
        {{block "content" .}}
        {{ end }}
    </main>
    {{ block "footer" .}}{{ end }}
</body>

</html>

{{ end }}